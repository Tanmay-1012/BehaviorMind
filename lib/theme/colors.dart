import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF00BFA5); // Teal
  static const secondary = Color(0xFF4A86E8); // Blue
  static const accent = Color(0xFFFF9800); // Orange

  // Neutrals
  static const white = Color(0xFFFFFFFF);
  static const lightGrey = Color(0xFFF2F2F2);
  static const grey = Color(0xFFB8B8B8);
  static const darkGrey = Color(0xFF545454);
  static const black = Color(0xFF000000);

  // Background colors
  static const lightBackground = Color(0xFFF5F7FA);
  static const darkBackground = Color(0xFF121212);
  static const darkSecondary = Color(0xFF1E1E1E);
  static const darkSurface = Color(0xFF2C2C2C);
  static const cardDark = Color(0xFF1E1E1E);

  // Status colors
  static const success = Color(0xFF4CAF50); // Green
  static const warning = Color(0xFFFFC107); // Amber
  static const error = Color(0xFFF44336); // Red
  static const info = Color(0xFF2196F3); // Light Blue

  // Chart colors
  static const chartBlue = Color(0xFF4A86E8);
  static const chartGreen = Color(0xFF00BFA5);
  static const chartOrange = Color(0xFFFF9800);
  static const chartPurple = Color(0xFF9C27B0);
  static const chartRed = Color(0xFFF44336);

  // Mood colors
  static const moodPositive = Color(0xFF4CAF50);
  static const moodNeutral = Color(0xFFFFC107);
  static const moodNegative = Color(0xFFF44336);
  static const moodMixed = Color(0xFF9C27B0);

  // Disabled state
  static const disabled = Color(0xFFE0E0E0);
  static const disabledDark = Color(0xFF424242);
}